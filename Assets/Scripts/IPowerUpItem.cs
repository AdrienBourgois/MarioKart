using UnityEngine;
using System;
using System.Collections;

public interface IPowerUpItem
{


    void AddBenefit();

    //    if (is_active)
      //      if (Time.time > time_action)
        //        StopActionOnPlayer(this, EventArgs.Empty);
}
